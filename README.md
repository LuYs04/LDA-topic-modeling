# LDA Topic Modeling

## Overview

This project implements **Latent Dirichlet Allocation (LDA)** to perform **topic modeling** on the 20 newsgroups text dataset. LDA is a probabilistic model used to discover latent topics in a collection of documents by analyzing word distributions.

The project includes:

- Data preprocessing (tokenization, stopword removal)
- LDA model training
- Discovering topics
